# Rust / Cargo
/target/
**/*.rs.bk
Cargo.lock.bak

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
/dist/
/release/
/release-artifacts/

# Local env
.env
.env.local

# Coverage
/coverage/
/lcov.info

# Web build outputs (Plan 5)
/web/dist/
/web/.trunk/

# Perf audit outputs (Plan 6) — keep reports, ignore intermediate raw data
/perf-harness/raw/

# Misc
*.log

# Claude Code / agent tooling (per-machine state, not part of the Rust build)
.claude/
.agents/
skills-lock.json

# Sibling worktrees (e.g., `.worktrees/v4` for the v4 reference during the
# Plan 11 manual parity sign-off). Created via `git worktree add`; never
# committed to the rewrite/bevy branch.
/.worktrees/

# Visual-companion brainstorming scratch (superpowers)
.superpowers/
