//! GPU compute for the Flame IFS: POD mirrors + branch/level encoding
//! (`sim_params`) and, from Task F6, the level-parallel dispatch pipeline.

// pub mod pipeline; // enabled in Task F6
pub mod sim_params;
