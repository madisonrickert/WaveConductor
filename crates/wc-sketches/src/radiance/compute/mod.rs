//! Render-world compute for the Radiance aura: GPU POD mirrors
//! ([`sim_params`]), the dispatch pipeline (`pipeline`, added by Task 6), and
//! the silhouette-edge storage-buffer upload (`edge_upload`, added by
//! Task 6).

pub mod sim_params;
